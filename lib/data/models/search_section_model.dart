import 'package:flutter/material.dart';

class SearchSectionLabel {
  IconData iconData;
  String label;
  Color color;

  SearchSectionLabel(
      {required this.iconData, required this.label, required this.color});
}
